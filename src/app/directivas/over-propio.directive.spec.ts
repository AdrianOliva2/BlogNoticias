import { OverPropioDirective } from './over-propio.directive';

describe('OverPropioDirective', () => {
  it('should create an instance', () => {
    const directive = new OverPropioDirective();
    expect(directive).toBeTruthy();
  });
});
